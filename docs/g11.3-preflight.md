# G11.3 an-Naziat Pre-flight Verification Record

Verified: 2026-05-09 05:00 UTC
Verifier: Claude Code (an-Naziat implementer)
Audit references: Round 33 (anticipated post-v0.12.0-ship);
v1.1 self-review absorption (eleven F-AN findings);
Round 34 cross-instance audit absorption (three F-PXR
polish items); the v1.2 Round 34 absorption resolves the
proposer-falsifier separation gap per methodology v1.1 §0.3
Calibration target: v0.12.0 al-Mursalat ship state

## 1. Version baseline

- pyproject.toml version: **0.11.7** (DIVERGED from
  expected 0.12.0)
- __init__.py version: **0.11.7** (DIVERGED from expected
  0.12.0)
- v0.12.0 merge SHA: **N/A; v0.12.0 has not shipped**

### CRITICAL DIVERGENCE -- ESCALATION TRIGGER

The an-Naziat prerequisite per Phase identification states:
"v0.12.0 shipped with three-substrate symmetry at substrate
+ dispatch (per al-Mursalat v1.2 acceptance criterion 17)."
The substrate at v1.2 ship time of this an-Naziat prompt
does NOT match this prerequisite: v0.11.7 is the highest
local-built version (al-Hujurat process-corrective
amendment, awaiting PR merge to origin); v0.12.0 al-Mursalat
HALTED AT T00 in the prior session per its own pre-flight
verification record at `docs/g11.2-preflight.md`. The
al-Mursalat T00 surfaced three Round 33+ finding seeds
(F4-substrate-architecture; F22-already-closed;
al-Hujurat T02 timestamp pending) and recommended re-scoping
of the al-Mursalat prompt before T01-T08 implementation.

Per Operating discipline halt-and-escalate clause and T00
step 1 mandate: **"if version is < 0.12.0, halt and
escalate."** This pre-flight record HALTS an-Naziat at
T00 step 1.

## 2. Test-count baseline

- v0.11.7 CHANGELOG claim: **408**
- pytest --collect-only -q 2>&1 | grep -E "tests? collected"
  output: **408 tests collected**
- Match: **YES**
- Calibration impact on T11 projection: not applicable
  pending al-Mursalat re-scoping; v0.13.0 projection cannot
  be calculated against an absent v0.12.0 baseline.

## 3. CASM-V code allocation (per F-AN-4 absorption)

| Code | First defined | Semantic |
|---|---|---|
| CASM-V-001 | v0.10.0 | unsupported language at step 2-3 |
| CASM-V-002 | v0.10.0 | unsupported manifest schema version |
| CASM-V-003 | v0.10.0 | manifest schema validation failure |
| CASM-V-010 | v0.10.0 | bundle parse error |
| CASM-V-020 | v0.10.0 | TUF trust root refresh failure (ADVISORY) |
| CASM-V-021 | v0.10.0 | sigstore verifier construction failure |
| CASM-V-030 | v0.10.0 | identity policy mismatch (issuer) |
| CASM-V-031 | v0.10.0 | identity policy mismatch (issuer URL) |
| CASM-V-032 | v0.11.0 | identity policy mismatch (subject) |
| CASM-V-033 | v0.10.0 | signature verification failure |
| CASM-V-034 | v0.10.0 | signed-payload tampering |
| CASM-V-035 | v0.11.0 | identity policy not supplied |
| CASM-V-036 | v0.11.0 | identity extraction failure (typed) |
| CASM-V-040 | v0.10.0 | checker_set_hash mismatch |
| CASM-V-050 | v0.10.0 | canonicalization drift |
| CASM-V-051 | v0.10.0 | canonicalization rule violation |
| CASM-V-060 | v0.10.0 | replay protection failure |
| CASM-V-061 | v0.10.0 | replay protection timestamp drift |
| CASM-V-070 | (in-substrate use TBD) | referenced in docs/g11.2-preflight.md only |
| CASM-V-100 | (in-substrate use TBD) | referenced in docs/g11.2-preflight.md only |

**Highest in-use code with substrate-level definition**:
CASM-V-061. CASM-V-070 and CASM-V-100 appear in
`docs/g11.2-preflight.md` as the al-Mursalat T00
verification record's "next available" reservations; they
are not yet used in src/ or tests/.

**T01 working hypothesis confirmation per F-PXR-1**:
**CASM-V-070 and CASM-V-071 are still next-available** for
ONNX opset-policy-mismatch and dim-param-violation. CASM-V-033
and CASM-V-034 remain in use at v0.10.0 for signature-
verification-failure and signed-payload-tampering; re-using
them would silently break those code contracts. The v1.2
working hypothesis holds against v1.2 ship-time substrate.

## 4. gate11 module inventory

Substrate state at v0.11.7 (al-Hujurat ship; al-Mursalat
NOT yet shipped):

- `__init__.py`: EXISTS
- `bundle.py`: EXISTS
- `checker_set_hash.py`: EXISTS
- `cli.py`: EXISTS
- `manifest_schema.py`: EXISTS
- `module_canonicalization.py`: EXISTS
- `signature_canonicalization.py`: EXISTS (Python)
- `signing.py`: EXISTS
- `surface_extraction.py`: EXISTS (Python)
- `verification.py`: EXISTS (handles BOTH python AND rust
  internally per v0.11.3 dispatch closure; line 157
  whitelist accepts `language in ("python", "rust")`)
- `rust_manifest.py`: EXISTS
- `rust_signature_canonicalization.py`: EXISTS
- `rust_surface_extraction.py`: EXISTS
- **`rust_verification.py`: ABSENT** (al-Mursalat T00 F4
  divergence; existing architecture is whitelist + helpers)
- **`go_verification.py`: ABSENT** (al-Mursalat halted at
  T00; T03-T08 not implemented)

The an-Naziat T00 step 4 expectation that
`rust_verification.py` and `go_verification.py` exist as
separate modules per al-Mursalat v0.12.0 ship is at
variance with the substrate state. The architecture
divergence cascades: an-Naziat T04 plans to author
`onnx_verification.py` in the per-language-module pattern,
but the existing substrate uses language whitelist +
helper-module pattern.

## 5. ONNX checker inventory (per F-PXR-2 absorption)

Substrate state at v0.11.7:

- `src/furqan_lint/onnx_adapter/__init__.py`: EXISTS
- `src/furqan_lint/onnx_adapter/numpy_divergence.py`: EXISTS
- `src/furqan_lint/onnx_adapter/numpy_reference.py`: EXISTS
- `src/furqan_lint/onnx_adapter/parser.py`: EXISTS
- `src/furqan_lint/onnx_adapter/probe_grid.py`: EXISTS
- `src/furqan_lint/onnx_adapter/public_names.py`: EXISTS
- `src/furqan_lint/onnx_adapter/runner.py`: EXISTS
  (multi-function surface containing D24-onnx +
  opset-compliance per F-PXR-2 working hypothesis;
  empirical confirmation pending grep for function names)
- `src/furqan_lint/onnx_adapter/score_validity.py`: EXISTS
- `src/furqan_lint/onnx_adapter/shape_coverage.py`: EXISTS
- `src/furqan_lint/onnx_adapter/translator.py`: EXISTS

ONNX adapter substrate is more developed than the v1.1
working hypothesis assumed. `src/furqan_lint/checkers/`
does NOT exist (F-PXR-2 confirmed); the F-PXR-2 v1.2
amendment correctly redirects T05 to `onnx_adapter/`
paths.

## 5b. ONNX fixture builders

- `tests/fixtures/onnx/__init__.py`: EXISTS
- `tests/fixtures/onnx/builders.py`: **EXISTS** (T10
  dependency confirmed; failure mode #5 of §5.1 step 4
  ranked list mitigated)
- `tests/fixtures/onnx/documented_limits/`: EXISTS with
  five fixtures already pinned (dynamic_shape_silent_pass,
  intermediates_excluded, numpy_divergence_neurogolf_convention,
  registry_pin_window, score_validity_optin_extra)
- `tests/fixtures/onnx/numpy_reference_examples/`: EXISTS
  with two fixture-build scripts

The v0.9.x ONNX work landed substantial fixture infrastructure
that an-Naziat can reuse. T10 fixture dependency on
`make_simple_relu_model` builder is unresolved without
inspecting builders.py contents; recommend T10
implementation phase-1 inspect the builder API rather than
assume the v1.0 reference signature.

## 6. Sigstore API pattern (live at v0.11.7; al-Mursalat
v0.12.0 not shipped)

Captured from `src/furqan_lint/gate11/verification.py`:

- **TrustedRoot import** (line 185-187, two-arm
  try/except per v0.11.5 F24 al-Bayyina hotfix):
  ```python
  try:
      from sigstore.trust import TrustedRoot  # public path
  except ImportError:
      from sigstore._internal.trust import TrustedRoot  # private fallback
  ```
- **Verifier import** (line 257):
  ```python
  from sigstore.verify import Verifier as SgVerifier
  ```
- **Bundle import** (line 256):
  ```python
  from sigstore.models import Bundle as SgBundle
  ```
- **VerificationError import** (line 255):
  ```python
  from sigstore.errors import VerificationError
  ```
- **Policy imports** (line 258+):
  ```python
  from sigstore.verify.policy import (...)
  ```
- **Verifier construction** (lines 308, 311, 313 with
  H-5 trusted_root threading and fallback):
  ```python
  try:
      verifier = SgVerifier(_inner=trusted_root)
  except Exception:
      verifier = (
          SgVerifier.production()
          if production_environment
          else SgVerifier.staging()
      )
  ```
- **API drift since v0.11.2 at-Tawbah**: YES (v0.11.5 F24
  al-Bayyina hotfix landed the public-path-preferred
  TrustedRoot import).
- **API drift since v0.11.7 al-Hujurat to v0.13.0 LIVE**:
  N/A; al-Mursalat v0.12.0 has NOT shipped, so the
  "v0.13.0 LIVE" pattern T04 is meant to mirror does not
  yet exist as substrate.

## 7. al-Mursalat amendments confirmation

Per al-Mursalat T02-T08 deliverables expected at v0.12.0:

- **CLI dispatch by `_VERIFIER_DISPATCH` dict** (al-Mursalat
  T02): **NOT IMPLEMENTED**. Dispatch lives inside
  `verification.py:157` as a language whitelist; not as a
  per-language dispatch dict.
- **`docs/gate11-symmetry.md`** (al-Mursalat T07):
  **ABSENT**.
- **Three smoke-test jobs** (al-Mursalat T08):
  gate11-rust-smoke-test EXISTS; gate11-python-smoke-test
  and gate11-go-smoke-test ABSENT.
- **F22 closure** (gate11-rust-smoke-test passes via
  dispatch routing): F22 was closed at v0.11.3 per the
  v0.11.3 release commit; smoke test passes via the
  language whitelist extension, not via dispatch table.

al-Mursalat amendments per the v1.2 prompt's specification
are NOT YET PRESENT at the substrate. Per the T00 step 7
mandate: **"If any of the four is absent or differs from
al-Mursalat v1.2 specification, halt an-Naziat and
escalate to project lead."** Three of the four are absent.

## 8. Round 33+ audit findings inventory

No Round 33 audit has been completed against v0.12.0
substrate (which has not shipped). The al-Mursalat T00
verification record's three Round 33+ finding seeds are
the canonical pre-Round-33 input set:

1. **F4-substrate-architecture** (CRITICAL for prompt
   re-derivation): rust_verification.py absent; existing
   verification.py handles language whitelist internally.
2. **F22-already-closed**: F22 was closed at v0.11.3
   per release commit; al-Mursalat narrative claim that
   v0.12.0 closes F22 is at variance with git log.
3. **al-Hujurat T02 timestamp pending**: v0.11.7 ship is
   on a local branch (not yet pushed to origin); T02
   branch protection rule's `gh api` call is pending
   project-lead post-merge action.

These three seeds remain open. an-Naziat layers on top
of an unshipped al-Mursalat foundation; the same
architecture divergence cascades: an-Naziat's T04 ONNX
verifier in the per-language-module pattern would
introduce a fourth such module before the architectural
question (single-verification.py-with-whitelist vs.
per-language-modules) has been resolved by project-lead
review.

## 9. Pre-flight verdict

- T00 status: **HALT-AND-ESCALATE**
- Reason: **Cascade halt**. an-Naziat prerequisite is
  v0.12.0 al-Mursalat ship; v0.12.0 has not shipped
  (al-Mursalat halted at T00 in the prior session).
  an-Naziat T00 step 1 mandate ("if version is < 0.12.0,
  halt and escalate") and step 7 mandate ("if any of the
  four al-Mursalat amendments is absent, halt and
  escalate") both fire. Step 4 substrate divergence (no
  rust_verification.py, no go_verification.py) compounds
  the architecture question that al-Mursalat T00 already
  surfaced.

### Round 35+ finding seeds (for the eventual external
audit on the layered chain)

1. **Cascade dependency surfaced**: an-Naziat T00 cannot
   proceed because its prerequisite (al-Mursalat v0.12.0
   ship) has not been satisfied. The prompt chain
   structure means each phase's substrate calibration
   depends on the prior phase's actual ship state, not
   the prior phase's prompt-projected ship state.
   Project-lead review of the al-Mursalat re-scoping
   recommendation is the unblocking step for an-Naziat.
2. **Architecture question unresolved**: al-Mursalat T00
   surfaced the per-language-modules vs.
   whitelist-with-helpers divergence; an-Naziat would
   author `onnx_verification.py` in the per-language-modules
   pattern, locking in the question's answer without
   project-lead review. Halting an-Naziat preserves
   project-lead choice.
3. **CASM-V-070 / CASM-V-071 reservations are clean**:
   the F-PXR-1 v1.2 working hypothesis holds. When
   al-Mursalat re-scoping completes and an-Naziat
   restarts, the codes are still available.
4. **ONNX adapter substrate is mature**: the v0.9.x
   work that landed `onnx_adapter/` modules + the
   documented_limits + numpy_reference_examples
   fixture infrastructure is more substantial than the
   an-Naziat v1.1 prompt anticipated. T05 path resolution
   per F-PXR-2 is correct against substrate; T10
   builder dependency per failure mode #5 is satisfied.
   When an-Naziat restarts, the substrate-side starting
   point is favorable.

### Recommendation

Per the Operating discipline halt-and-escalate clause,
T00 step 1 mandate ("if version is < 0.12.0, halt and
escalate"), and T00 step 7 mandate (al-Mursalat amendments
absent halt-and-escalate), this pre-flight record HALTS
an-Naziat at T00 and escalates to project lead with the
four Round 35+ finding seeds.

The unblocking sequence:

1. Project lead reviews the al-Mursalat T00 verification
   record at `docs/g11.2-preflight.md` and the three
   Round 33+ finding seeds it surfaced.
2. Project lead disposes the architecture question:
   either (a) accept the existing language-whitelist
   pattern and re-scope al-Mursalat to extend the
   whitelist for Go (smaller scope; +15-20 tests rather
   than the prompt's projected +22), or (b) authorize a
   refactor of `verification.py` into
   per-language-verifier modules as a separate corrective
   phase before al-Mursalat substrate work proceeds.
3. al-Mursalat (re-scoped per option a or b) ships at
   v0.12.0 with the dispatch architecture resolved.
4. an-Naziat restarts with v0.12.0 in place; the v1.2
   prompt's T04 architectural pattern is either accepted
   (option b chosen above) or re-derived to match the
   whitelist pattern (option a).

The substrate is closer to v0.13.0 than the layered prompt
chain assumes (mature ONNX adapter, viable T05 paths,
viable T10 fixture builder), but the prerequisite chain
is unresolved.
